import { createMuiTheme } from "@material-ui/core/styles";

/*
const theme = createMuiTheme({
  palette: {
    type: "dark",
    /*primary: {
      main: "#455a64",
    },
    secondary: {
      main: "#eceff1",
    },*//*
  },
});
*/
const theme = createMuiTheme({
  palette: {
    type: 'dark',
  },
});

export default theme;
