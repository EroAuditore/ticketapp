import React, { Component } from "react";
import Tickets from "./Tickets";

const Details = props => {
  return (
    <React.Fragment>
      <Tickets></Tickets>
    </React.Fragment>
  );
};

export default Details;
