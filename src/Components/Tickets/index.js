import React from "react";
import Tickets from "./Tickets";

const Details = (props) => {
  return (
    <React.Fragment>
      <Tickets></Tickets>
    </React.Fragment>
  );
};

export default Details;
